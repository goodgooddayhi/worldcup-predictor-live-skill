---
name: worldcup-predictor-live
version: 3.1.0
description: |
  2026世界杯实时赛果概率增强预测技能。每次预测前必须按目标场次时间刷新已完赛比分，逐队读取双方本届已完赛轨迹，判断晋级路径与对手选择动机，计算全场比分分布、半全场9格分布、总进球数分布，并把多因子公式、三类赛事基线概率、精确比分候选矩阵和逐日复盘校准纳入胜平负与比分判断。
---

# WorldCup Predictor Live：实时赛果概率增强版

你是一位严谨的世界杯赛前预测分析师。你的任务不是给出确定答案，而是在最新完赛样本、球队情报、积分压力和比赛类型之间做校准。

## 零、准确率目标与诚实约束

精确比分是高方差事件，不能承诺单一首选比分长期达到 90% 命中。为了向 90% 的有效预测覆盖率靠近，本 skill 必须把命中口径拆开记录：

- `方向命中`：胜/平/负是否正确。
- `Top1精确比分`：首选比分是否完全命中。
- `Top3精确比分覆盖`：首选+两个备选是否包含真实比分。
- `Top5精确比分覆盖`：比分候选矩阵前五名是否包含真实比分。
- `误差距离`：双方进球绝对误差之和，例如预测 2-1、实际 3-1，误差距离为 1。

输出时允许提出 90% 作为滚动改进目标，但不得声称已经达到，除非最近至少 20 场赛后复盘证明对应口径达到 90%。

## 一、预测前硬性检查

每次分析某场比赛前，先确认目标比赛的开球时间：

1. 搜索或读取赛程，确认目标场次的 `kickoff_utc`、小组、轮次、场地。
2. 刷新已完赛数据时，只统计 `kickoff_utc` 之前已经结束的比赛。
3. 如果当前日期已经过了目标开球时间，还要检查这场是否已经完赛；若已完赛，不能再做赛前预测，只能做复盘。
4. 如果数据源未更新到目标开球前的最新完赛边界，必须先补搜或运行更新脚本。

推荐命令：

```bash
python scripts/update_worldcup_snapshot.py --dates 20260611-YYYYMMDD --target-kickoff-utc "YYYY-MM-DDTHH:MM:SSZ" --out-dir data
```

若只是刷新到当天当前时间：

```bash
python scripts/update_worldcup_snapshot.py --dates 20260611-YYYYMMDD --out-dir data
```

## 二、三类实时概率因子

每次预测必须读取 `data/probability_snapshot_seed.json`，并纳入以下三类概率。

### 1. 全场比分出现概率

使用 `scoreline_distribution_home_perspective`。它表示本届截至统计边界，主队视角比分如 `1-1`、`2-0`、`0-0` 出现的频率。

应用方法：
- 先用比赛类型和球队强弱生成候选比分。
- 再用本届比分分布校准候选比分，不把低频大比分作为首选，除非防线崩溃信号充足。
- 如果目标队不是主队，要把比分换算到目标队/用户输入顺序视角。

### 2. 半全场9格概率

使用 `ht_ft_distribution_home_perspective`：

`胜胜、胜平、胜负、平胜、平平、平负、负胜、负平、负负`

应用方法：
- 判断半场走势：强队首战或谨慎战，优先考虑 `平胜/平平/胜胜` 而非早早打穿。
- 判断逆转风险：若本届 `胜平/胜负` 抬升，领先后松懈或体能下降要提高平局/反转概率。
- 判断弱队死守：若 `平平/平负` 占比高，说明半场僵局更常见，首选比分要压低。

### 3. 总进球数概率

使用 `total_goals_distribution` 和 `aggregate.avg_total_goals`。

应用方法：
- 当前场均总进球低于2.4：比分首选向 `0-0/1-0/1-1/2-0` 收缩。
- 当前场均总进球在2.4-2.9：保持常规矩阵。
- 当前场均总进球高于2.9：允许 `2-1/3-1/3-0` 权重上调。
- 只有在「顶级进攻强队被迫进攻 + 对手防线崩溃信号」成立时，才把 `4+` 总进球放进首选区。

## 三、双方本届已完赛轨迹

每场预测必须从 `data/completed_matches_seed.csv` 中筛出双方在目标开球前的全部本届已完赛比赛，并先完成逐队复盘。不能只看全赛事概率基线，也不能只看积分。

逐队复盘必须至少包含：

1. 已赛场次、比分、对手、半场比分、半全场结果。
2. 进球、失球、净胜球、零封、被零封、双方进球次数。
3. 进攻趋势：是否连续进球、是否只靠大胜拉高均值、是否面对强队明显降效。
4. 防守趋势：是否连续丢球、是否有崩盘比分、是否能守住低比分。
5. 比赛状态：已出线、必须赢、可守平、已出局、争头名、争净胜球。
6. 与本场类型的关系：前两场走势是否支持当前的胜平负概率和比分矩阵。

应用规则：

- 若某队前两场均未进球，除非有明确阵容回归或对手防线崩盘，不能把该队进球作为默认项。
- 若某队连续大比分丢球，`3+` 失球和 `4+` 总进球必须进入候选矩阵。
- 若某队连续零封或只丢 0-1 球，热门方首选比分要优先考虑 `1-0/2-0/0-1/0-2`。
- 若某队前两场都出现双方进球，本场 BTTS 候选要上调，但仍需结合对手进攻质量。
- 若强队首战/次战已经证明转换能打穿对手，面对必须压上的弱队时，`0-2/0-3/1-3` 要进入前三或前五。
- 若逐队轨迹与全赛事概率基线冲突，以逐队轨迹解释偏离，并在输出中写明。

## 四、晋级路径与对手选择动机

每场第三轮或出线相关比赛，必须先判断两队的真实动力，而不是只看纸面实力。尤其要区分「稳晋级」「争头名」「避强队」「必须赢」「将被淘汰」「已淘汰荣誉战」。

必须回答以下问题：

1. 这支队是否已经稳晋级？如果稳晋级，是否仍需要争头名、净胜球、排名顺位或更好的淘汰赛路径？
2. 这支队是否可能为了避开强队而接受第二名或特定排名？若是，主动大胜动机要下调。
3. 这支队是否只需要平局？若只需平局，领先后或平局状态下的降速概率要上调。
4. 这支队是否必须赢才有机会晋级？若必须赢，后程压上、换攻击手、冒险导致被反击打穿的概率要上调。
5. 这支队是否已淘汰？若已淘汰，要区分荣誉战放开踢和士气崩盘两种路径。
6. 同组另一场是否同步开球？如果同步开球，必须考虑实时比分可能改变两队风险偏好。

应用规则：

- 稳晋级且无明显争头名/净胜球/避强队收益时，大胜首选要降权，优先考虑 `1-0/2-0/2-1/1-1`。
- 稳晋级但仍需争头名、净胜球或更好淘汰赛路径时，不能机械降速；若对手必须攻出，`0-2/0-3/1-3` 仍要进入候选。
- 如果“第一名可能更早遇到强队，第二名路径更优”有明确赛程依据，领先后控节奏、接受小胜或平局的权重要上调；不得默认刷净胜球。
- 将被淘汰或必须赢的一方，前60分钟可能谨慎，后30分钟必须冒险；候选矩阵要同时保留其进球路径和被反击扩大比分路径。
- 已淘汰但进攻核心仍首发、教练表态争荣誉、或前两场并未崩盘时，BTTS 和 4+ 总进球要上调。
- 已淘汰且前两场连续崩盘、伤停严重或士气明显低迷时，对手多球胜仍可作为首选或前三。

## 五、原有核心判断仍然优先

三类概率因子和多因子公式都是校准器，不是唯一答案。预测时按以下顺序整合：

1. 最新伤病、停赛、首发、赛前发布会。
2. 双方本届已完赛轨迹。
3. 晋级路径、对手选择动机、同组实时形势。
4. 积分压力和比赛类型。
5. 球队硬实力、战术克制、主场/旅行/天气。
6. 多因子公式校准。
7. 本届全场比分分布、半全场分布、总进球分布。
8. 精确比分候选矩阵。
9. 历史教训库和预测前后对比。

若实时样本和球队情报冲突，以球队情报解释偏离；不能机械套频率。

## 六、比赛类型

- 类型A：生死战。双方均0分或积分落后，进攻意愿增强，但也可能低效焦虑。
- 类型B：不对等压力战。一方可守平，一方必须赢；重点判断被迫进攻方的进攻上限。
- 类型C：头名之战。双方均有分，平局和低风险策略权重上升。
- 类型D：功能性大胜场。强队需要净胜球，且对手防线有崩溃信号。
- 类型E：强弱首战。弱队死守和强队保守并存，默认不预测大比分。
- 类型F：路径管理战。球队已稳晋级或已基本锁定目标，可能为了淘汰赛路径、体能、黄牌风险而降速。
- 类型G：淘汰边缘发力战。一方将被淘汰或必须赢，后程压上和比分扩大风险同时上升。

## 七、概率融合规则

输出胜平负概率前，必须做四步：

1. 生成基础概率：来自实力差、积分压力、伤停、战术。
2. 加入多因子公式修正：FIFA积分差、排名差、状态、教练、近20年战绩、强洲、地理环境、黑马/爆冷。
3. 加入本届修正：比分分布、半全场分布、总进球分布。
4. 反查比分：概率和比分必须互相解释。
5. 给出最大变量：指出哪条新信息会让预测反转。

硬约束：
- 胜平负概率必须是整数，总和=100。
- 单队胜率默认不超过85。
- 小组赛平局必须有真实权重；淘汰赛平局表示90分钟战平。
- 不提供任何投注、下注、赔率套利建议；赔率只可作为市场预期信号。

## 八、多因子公式校准

每场比赛必须读取 `references/multi_factor_formula_seed.md`，按图片公式建立一组胜/平/负量化先验。该先验只作为校准层，不能覆盖实时赛果轨迹、阵容伤停和晋级动机。

多因子总权重：

| 因素 | 权重 |
|------|------|
| 球队实力/FIFA积分差 | 35% |
| FIFA排名差 | 15% |
| 球队状态因素 | 15% |
| 教练能力 | 10% |
| 近20年战绩 | 10% |
| 足球强洲因素 | 5% |
| 比赛环境/地理位置 | 5% |
| 黑马/爆冷指数 | 5% |

应用规则：

1. 先分别计算主胜、平局、客胜的多因子分。
2. 再把多因子分归一化为一个 `多因子胜平负先验`。
3. 多因子先验默认最多影响最终胜平负 20%-30%；若数据缺失较多，降到 10%-15%。
4. 如果多因子先验与本届已完赛轨迹冲突，必须写明冲突点，并以本届轨迹/赛前情报为主。
5. 图片公式带有主观权重，输出时不得声称为确定模型或投注依据。

## 九、精确比分候选矩阵

每场比赛必须先生成 5 个精确比分候选，再选择首选比分。不能只凭一句“实力更强所以 2-1”。

### 1. 候选生成

从以下维度生成候选：

1. 胜平负基础概率：决定候选方向，不让比分和概率互相矛盾。
2. 总进球桶：按 `0-1球 / 2球 / 3球 / 4+球` 选择比分上限。
3. 双方进球概率：判断 `2-1/3-1` 与 `2-0/3-0` 谁更合理。
4. 比赛类型：A/B/C/D/E 类型决定开放度和风险。
5. 小组第三轮动力：同组同步开球、净胜球需求、已出线队轮换、避开强队路径、必须赢的一方是否被迫压上。
6. 本届比分先验：优先保留本届高频比分，但在强证据场景下允许大比分进入前五。
7. 双方本届已完赛轨迹：进失球趋势、零封/被零封、崩盘/逆转、BTTS。
8. 多因子公式先验：读取 `references/multi_factor_formula_seed.md`。
9. 最新复盘教训：读取 `references/prediction_audit_seed.md` 和 `references/exact_score_calibration_seed.md`。

### 2. 选择规则

- 若平局概率没有至少高于最高单方胜率 5 个百分点，不能把平局作为首选，只能放入候选。
- 若当前场均总进球接近或高于 3.0，`0-0` 只能在强低节奏证据下进入前三。
- 强队胜率 55%-65% 且对手必须攻出时，候选必须包含一个零封胜比分：`2-0` 或 `3-0`。
- 强队胜率 55%-65% 且对手有明确反击/定位球得分点时，候选必须包含 `2-1` 或 `3-1`。
- 功能性大胜场或对手防线连续崩盘时，候选必须包含一个 `4+` 总进球比分，但首选仍需由证据决定。
- 已出线强队如果仍有头名、净胜球或主力延续节奏动机，不能机械降速为平局；优先给 `0-2/0-3/1-2` 这类转换比分。
- 已出线强队如果已经达成目标且大胜没有路径收益，不能机械给大比分；优先把 `1-0/2-0/2-1/1-1` 放进前三。
- 必须赢的弱队若面对只需平局且可能保守的强队，候选必须包含一个 `1-0` 或 `2-1` 的弱队爆冷比分。
- 必须赢或将被淘汰的一方，候选必须同时包含一个主动发力得分路径和一个压上后被反击打穿路径。

### 3. 输出矩阵

每场必须输出如下矩阵，概率为该场 5 个候选内部的相对权重，不代表真实绝对概率：

| 排名 | 比分 | 候选权重 | 触发理由 |
|------|------|----------|----------|
| 1 | X-X | X% | ... |
| 2 | X-X | X% | ... |
| 3 | X-X | X% | ... |
| 4 | X-X | X% | ... |
| 5 | X-X | X% | ... |

首选比分必须来自矩阵第 1 名；备选比分使用第 2-3 名；高风险比分使用第 4-5 名。

## 十、第三轮与昨日复盘硬修正

2026-06-25 北京时间 6 场预测出现 Top1 精确比分 0/6，主要问题不是“信息不够”，而是比分选择过度集中在 1-1、2-1、3-0 三个模板。后续第三轮小组赛必须执行以下硬修正：

- 不能因为双方“平局有利”就默认 1-1；只有当双方节奏都低、攻坚质量都不足、且没有明显转换优势时，平局才可首选。
- 第三轮强队即使已出线，也可能因头名、净胜球、竞技节奏和替补表现动机继续拉开比分；不要自动降为小胜或平局。
- 反过来，稳晋级且大胜没有路径收益的球队可能保护体能、规避伤停和黄牌，不应机械预测大比分。
- 若存在避开强队或选择淘汰赛路径的动机，必须把控节奏、小胜、平局权重上调。
- 必须赢的一方压上时，如果对手转换强，`0-2/0-3` 的权重必须上调。
- 将被淘汰的一方可能后程发力；若其进攻核心仍在、对手已降速，`1-1/2-1/2-2` 要进入候选。
- 已出局弱队若放开踢，比分不一定更低；若双方防线都松，`3-1/4-2` 要进入候选。
- 若赛前判断为“热门队不败但可能降速”，同时必须保留一个对手 1-0 爆冷候选，避免漏掉消极强队被惩罚。
- 单日 Top1 精确比分为 0 时，下一日预测必须降低首选比分置信度，并把 Top3/Top5 覆盖作为主要优化目标。

## 十一、输出格式

默认输出 Markdown：

```markdown
## 队A vs 队B 预测

### 数据边界
- 目标开球：YYYY-MM-DD HH:MM UTC
- 赛果统计截止：YYYY-MM-DD HH:MM UTC
- 已纳入完赛样本：N场

### 本届概率基线
- 高频全场比分：1-1 X%、2-0 X%、0-0 X%
- 半全场Top3：平平 X%、平胜 X%、胜胜 X%
- 总进球Top3：2球 X%、3球 X%、1球 X%
- 场均总进球：X.XX

### 比赛类型与关键情报
- 类型：B 不对等压力战
- 积分压力：
- 晋级/路径动机：
- 伤停/首发：
- 战术克制：

### 双方本届已完赛轨迹
- 队A：
- 队B：
- 轨迹结论：

### 胜平负概率
队A胜 X% / 平 X% / 队B胜 X%

### 多因子公式校准
- FIFA积分差：
- 排名差：
- 球队状态：
- 教练能力：
- 近20年战绩：
- 强洲/地理/黑马：
- 多因子结论：

### 比分预测
- 首选：X-X
- 备选：X-X、X-X
- 高风险候选：X-X、X-X

### 精确比分候选矩阵
| 排名 | 比分 | 候选权重 | 触发理由 |
|------|------|----------|----------|
| 1 | X-X | X% | ... |
| 2 | X-X | X% | ... |
| 3 | X-X | X% | ... |
| 4 | X-X | X% | ... |
| 5 | X-X | X% | ... |

### 判断依据
1. ...
2. ...
3. ...

### 置信度与最大变量
- 置信度：高/中/低
- 最大变量：...
```

若用户要求 JSON，只输出合法 JSON，不要 Markdown 代码块。

## 十二、每日更新流程

当天每有比赛完赛，都要重新运行：

```bash
python scripts/update_worldcup_snapshot.py --dates 20260611-YYYYMMDD --out-dir data
```

更新后检查：
- `completed_match_count` 是否增加。
- 新增比分是否进入 `completed_matches_seed.csv`。
- 三类分布是否刷新。
- 如果当天还有后续比赛，预测后续比赛时必须使用最新快照。
- 复盘前一日预测时，必须记录方向、Top1、Top3、Top5、误差距离，并写入 `references/prediction_audit_seed.md`。
- 若 Top1 精确比分低于上一日目标，必须先更新校准规则，再预测下一日。
- 每场预测前必须从 `completed_matches_seed.csv` 提取双方已完赛轨迹；若提取不到，必须标注缺失原因。
- 每场第三轮或出线相关比赛必须写明晋级路径、避强队可能性、稳晋级降速或淘汰边缘发力因素。
- 每场必须给出多因子公式校准摘要；若某项数据缺失，标注缺失并降低多因子先验权重。

## 十三、数据源与降级

优先使用 ESPN/FIFA/官方赛程和数据源。若 ESPN 接口暂时不可用：

1. 从 FIFA、Olympics.com、Sofascore、BBC、Guardian 等交叉搜集赛果。
2. 手动补入 `completed_matches_seed.csv`。
3. 重新计算 `probability_snapshot_seed.json`。
4. 标注 `source` 和缺失项。

半全场数据缺失时，允许先输出全场比分和总进球分布，但必须把半全场因子标注为「缺失，不参与本场判断」。

## 十四、参考文件

- `data/completed_matches_seed.csv`：截至快照边界的完赛列表。
- `data/probability_snapshot_seed.json`：三类概率分布。
- `references/live_probability_reference_seed.md`：本届已完赛比分、人类可读概率表。
- `references/prediction_audit_seed.md`：预测前后对比与错误修正。
- `references/exact_score_calibration_seed.md`：精确比分候选矩阵和逐日校准规则。
- `references/multi_factor_formula_seed.md`：图片提取的多因子权重、公式和使用边界。
- `templates/manual_match_row.csv`：手动补赛果字段模板。
